/*
Things to keep in mind:
- For fast input and output, use below before any cin or cout;
    ios_base::sync_with_stdio(false);

- unties input and output stream:
    cin.tie(NULL);

- use "\n" instead of endl; (flush proof and faster)

- use the following header file to include all standard libraries(g++ specific)

    #include<bits/stdc++.h>
- use string str; getline(cin, str) to read the whole lines of strings with spaces

- use typedef to further save time :)
    typedef long long ll;
    typedef vector<int> vi;
    typedef vector<vi> vii;
    typedef vector<ll> vlli;
    typedef pair<int, int> pii;
    typedef pair<long long> pll;
    typedef vector<pii> vpii;

- use macros to save time;

    
*/

#include <bits/stdc++.h>

#define REP(i, x, y) for(int i = x; i < y; i++)
#define F first
#define S second
#define EB emplace_back

using namespace std;

typedef long long ll;
typedef vector<int> vi;
typedef vector<vi> vii;
typedef vector<ll> vlli;
typedef pair<int, int> pii;
typedef pair<long, long> pll;
typedef vector<pii> vpii;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    for(int i = 0; i < t; i++){
        //read input for a test case
        //implement logic
        //write output for the test case

    }
    return 0;
}